# redstone_accounting

install dependencies in virtual environment

```shell
poetry install
```

start virtual environment

```shell
poetry shell
```

add new dependency

```shell
poetry add {dependency_name}
```
